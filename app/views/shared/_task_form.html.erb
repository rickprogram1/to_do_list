<%= form_for(@task) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <%= f.text_area :content, placeholder: "Add your task", class: "form-inline" %>
  <%= button_tag type: "submit", class: "btn btn-primary2" do %>
   <i class="fas fa-plus fa-post"></i>
  <% end %>
<% end %>